# 2026-04-19 worktree 整理与处置记录

## 1. 处置原则

本次整理采用“归档后清理”的策略：先把各 worktree 的 HEAD、分支、工作区状态、已提交差异、未提交差异与未跟踪文件清单归档到本地 `logs/`，再删除不继续推进的 worktree。该归档目录属于本地运行产物，不纳入 Git 版本控制。

主工作区的 4 月 19 日修复已先通过全量测试并提交，基线提交为：

- `9822c06167f10ac76f27605c049c0eb84cac3c79`
- `fix: complete workspace stabilization plan`

## 2. 归档位置

本轮 worktree 归档目录：

- `logs/worktree_archive/20260419T105057Z`

归档清单文件：

- `logs/worktree_archive/20260419T105057Z/archive_manifest.tsv`

每个 worktree 子目录均包含：

- `head.txt`
- `branch.txt`
- `status.txt`
- `committed_diff_stat.txt`
- `committed_branch.patch`
- `worktree_diff_stat.txt`
- `worktree_uncommitted.patch`
- `untracked_files.txt`
- `untracked_files.tar.gz`，仅当存在未跟踪文件时生成。

## 3. worktree 处置结果

| worktree | 分支 | HEAD | 状态 | 处置 |
| --- | --- | --- | --- | --- |
| `candidate-table-v2` | `candidate-table-v2` | `c4558a44aa1dfae0eebcef87017f4deff747b3bf` | 有未提交差异，无未跟踪文件 | 保留为唯一活跃研究 worktree |
| `candidate-table-v2-recall` | `candidate-table-v2-recall` | `64fb4980c2ae6f26eafed96ea399b6e31a79d1ad` | 干净 | 已归档并移除 worktree |
| `event-probe-v1` | `event-probe-v1` | `19aa419031b1d9e5b2435e2546b70256a7478c28` | 有未提交差异 | 已归档并移除 worktree |
| `event-probe-v2-error-driven` | `event-probe-v2-error-driven` | `665480cfec748b5ccc1873eb7e49c08e4ca5224c` | 有未提交差异 | 已归档并移除 worktree |
| `record-assembly-v1` | `record-assembly-v1` | `b1125318d19b696535c1fc3917a57696b62e73e2` | 有未提交差异 | 已归档并移除 worktree |
| `server-storage-migration` | `server-storage-migration` | `c82345b473077a54504179c82825281f6f073c4c` | 有未提交差异 | 有效内容已合入 `ENGINEERING_STATUS.md`，worktree 已归档并移除 |
| `sft-distill-role-span-table` | `sft-distill-role-span-table` | `33004c575b1497d12258148db8f3c9d179c793d2` | 有未提交差异和未跟踪文件 | 已归档并移除 worktree |
| `structured-sft-pipeline` | `structured-sft-pipeline` | `b98d1f8fc203ce022117599658b97bd053ae7065` | 有未提交差异和未跟踪文件 | 已归档并移除 worktree |

## 4. 合入主线的通用修复

- `ConfigManager` 现在会忽略 `torchrun` 注入的 `--local-rank=...`、`--local_rank=...`、`--local-rank ...` 和 `--local_rank ...`，避免分布式启动参数破坏业务 CLI override。
- `ENGINEERING_STATUS.md` 已补充 4090 服务器大文件存放约定，明确 `/data/TJK/og-lans/data` 与 `/data/TJK/og-lans/models` 作为服务器侧数据和模型规范根目录。

## 5. 当前工作区状态

当前保留的 worktree：

- 主工作区：`/home/tjk/myProjects/internship-projects/og-lans`
- 研究 worktree：`.worktrees/candidate-table-v2`

`candidate-table-v2` 的 `.venv`、pytest 缓存、Python `__pycache__` 和 egg-info 已清理；目录占用从约 `5.4G` 降至约 `2.9M`。

## 6. 验证记录

- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q`
  - 在整理前的主线基线提交前执行，结果为 `320 passed, 18 skipped, 2 warnings`。
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_config.py -q`
  - 在合入 launcher 参数过滤后执行，结果为 `12 passed, 2 warnings`。
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q`
  - 在 worktree 归档、清理和整理记录生成后执行，结果为 `321 passed, 18 skipped, 2 warnings`。
